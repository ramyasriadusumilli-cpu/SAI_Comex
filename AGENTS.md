# AGENTS.md

See [`CLAUDE.md`](CLAUDE.md) — same repository, same rules, one file. This
stub exists only because some tooling looks for `AGENTS.md` specifically.

Read `CLAUDE.md` first. It links out to `docs/` for anything that does not
fit in a single orientation file.
